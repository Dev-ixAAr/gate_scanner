// ============================================================================
// Ticket Scan Screen — Camera view for scanning attendee tickets
//
// Full implementation in Phase 7.
// Phase 1: Placeholder widget.
// ============================================================================

import 'package:flutter/material.dart';

/// Full-screen camera view for scanning attendee ticket QR codes.
///
/// TODO: Implement in Phase 7
class TicketScanScreen extends StatelessWidget {
  const TicketScanScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'TicketScanScreen\nPhase 7',
          textAlign: TextAlign.center,
          style: TextStyle(color: Colors.white),
        ),
      ),
    );
  }
}